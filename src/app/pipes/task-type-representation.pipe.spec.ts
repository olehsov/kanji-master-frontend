import { TaskTypeRepresentationPipe } from './task-type-representation.pipe';

describe('TaskTypeRepresentationPipe', () => {
  it('create an instance', () => {
    const pipe = new TaskTypeRepresentationPipe();
    expect(pipe).toBeTruthy();
  });
});
